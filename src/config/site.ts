/**
 * TIYATROTIST — Global Site Configuration
 * Toggle maintenanceMode to true to put the entire site into Maintenance Mode.
 */

export const SITE_CONFIG = {
  /**
   * Set this to `true` to put the entire site in Maintenance Mode.
   * Set to `false` for normal website operation.
   */
  maintenanceMode: false,
};
